def handle_user(req) {
    show($"User route accessed! Param ID: {req.params.id}")
    return $"User {req.params.id}"
}

set routes = {
    "GET /users/:id": handle_user
}

http_serve(8080, routes, "public")
